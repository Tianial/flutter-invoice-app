import 'package:flutter/material.dart';
class IngoingInvoicePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Eingangsrechnungen'),
      ),
      body: Center(
        child: Text('it works too'),
      ),

    );
  }
}
