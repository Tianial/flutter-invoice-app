import 'package:flutter/material.dart';
class FirmInformationsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Geschaeftinformationen'),
      ),
      body: Center(
        child: Text('it works'),
      ),

    );
    
  }
}
